"""
__add__：Python 魔法方法，对应 **+运算符 **，当执行实例1 + 实例2时，底层会调用实例1.__add__(实例2)；
__class__
__init__：初始化方法，创建实例后自动调用，用于初始化实例属性（如def __init__(self, name): self.name = name）；
__new__：构造方法，比__init__更早执行，用于创建实例对象（极少重写，仅在不可变对象 / 单例模式中使用）；
__del__：析构方法，实例被垃圾回收时自动调用，用于释放资源（一般无需手动实现，Python 自动管理内存）。
"""